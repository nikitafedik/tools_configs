#!/usr/bin/env bash
# rng - thin wrapper to launch ranger file manager.
# Usage: rng [path]
# Passes all arguments through to ranger.
# Exits with ranger's exit code.

if ! command -v ranger >/dev/null 2>&1; then
  echo "rng: ranger not found in PATH" >&2
  exit 127
fi

exec ranger "$@"
